# Classification déterministe des fichiers SQL du dépôt — environnement local
# Révisé le 2026-07-22 après analyse frontière snapshot/migrations.
#
# Cinq statuts possibles, un seul par fichier :
#   BASELINE MAÎTRE                        -> appliqué toujours, par défaut
#   MIGRATION PRODUIT                      -> appliquée toujours, par défaut, APRÈS le baseline, dans l'ordre listé
#   OVERLAY MANUEL FACULTATIF              -> appliqué uniquement sur commande explicite (--with-rgpd)
#   HISTORIQUE ABSORBÉ — NE PAS REJOUER    -> documentation seule, jamais exécuté par les scripts
#   SCRIPT DE TEST — NE PAS APPLIQUER      -> jamais un objet de schéma, exécuté séparément comme test
#
# Preuve de la classification : voir chaque ligne ci-dessous + l'analyse
# complète dans _architecture/PHASE_1A_CHECKPOINT.md (section T0/environnement local).
#
# RÈGLE ACTÉE EN PHASE 1C (2026-07-22) : à partir de maintenant, le baseline
# ci-dessus reste FIGÉ. Toute nouvelle correction produit devient une entrée
# de la section [PRODUCT-MIGRATIONS] ci-dessous -- jamais fusionnée dans
# supabase-schema.sql. Reconstruction cible : baseline figé + migrations
# produit ordonnées + overlay RGPD uniquement sur demande explicite.

# ══════════════════════════════════════════════════════════════════
# BASELINE (appliqué par défaut, sans option)
# ══════════════════════════════════════════════════════════════════
[BASELINE]
supabase-schema.sql
# Commit de référence : 51d55ffb41d1a48e68ba6467def8614165b1e718 (2026-07-20 11:05:53 +0200)
# Voir scripts/local-db/BASELINE_MANIFEST.txt pour le hash exact et la règle de revalidation.

# ══════════════════════════════════════════════════════════════════
# MIGRATIONS PRODUIT (appliquées par défaut, dans l'ordre listé, APRÈS le baseline)
# Jamais fusionnées dans supabase-schema.sql -- chaque fichier reste une
# migration à part entière, même après application en production.
# ══════════════════════════════════════════════════════════════════
[PRODUCT-MIGRATIONS]
migrations/2026-07-22-fix-t2-onboarding-sector-idempotence.sql
# T2 : corrige la divergence de secteur (menage/conciergerie/maintenance/autre)
# et rend create_profile_and_company idempotente/sûre en cas d'appel concurrent.
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-23-client-portal-data-rls.sql
# Portail client : ajoute get_my_client_devis()/get_my_client_factures()/
# get_my_client_interventions() (RPC SECURITY DEFINER, lecture seule) --
# devis/factures/interventions vivent dans seba_state.state (JSONB par
# patron), sans policy SELECT directe possible sans fuiter tout le patron.
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-23-employee-portal-missions.sql
# Portail employé : ajoute get_my_employee_interventions() (sans argument,
# toutes dates, enrichi de l'adresse client) et
# update_my_employee_intervention_status(p_intervention_id, p_status)
# (en_cours/terminee uniquement, verrouillage FOR UPDATE, ne touche que le
# statut). N'altère pas get_my_employee_interventions(_date text) ni
# close_my_intervention(...) (baseline, sections 35/36) -- ajouts purs.
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-24-mission-field-report.sql
# Retour terrain structuré (submit_my_intervention_field_report) : ajoute
# intervention.fieldReport (outcome/issueType/followUp... valeurs
# controlées côté serveur), ajout pur, ne modifie pas close_my_intervention
# (baseline). Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-25-intervention-360.sql
# Intervention 360 (feature/intervention-360) : 9 RPC employé
# (get_my_employee_intervention_detail, start/pause/resume_my_intervention,
# update_my_intervention_checklist, add_my_intervention_material,
# submit_my_intervention_incident, add_my_intervention_photo,
# complete_my_intervention) + 4 RPC client
# (get_my_client_intervention_detail, request_my_intervention_reschedule,
# approve_my_completed_intervention, report_my_intervention_issue) +
# bucket privé intervention360-photos (RLS patron/employé
# assigné/client si visibleToClient). Ajouts purs, ne modifie aucune RPC
# existante. Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-26-quote-to-cash.sql
# Quote-to-cash (feature/quote-to-cash) : 4 RPC client nouvelles
# (get_my_client_devis_detail, get_my_client_facture_detail,
# client_accept_devis, client_refuse_devis) + resserrement en allowlist de
# get_my_client_devis()/get_my_client_factures() (renvoyaient la ligne
# brute depuis 2026-07-23, devenu une fuite depuis que devis/factures
# portent notes patron + historique + paiements notés). Voir l'en-tête du
# fichier pour le détail complet.

migrations/2026-07-26-team-availability-suggestions.sql
# Team availability + suggestions (feature/team-availability-suggestions) :
# 2 RPC employé nouvelles (create_my_unavailability_request,
# cancel_my_unavailability_request). Aucune nouvelle RPC de lecture --
# get_my_employee_profile() (baseline) renvoie déjà l'objet employé complet,
# unavailabilityRequests y est donc déjà exposé. Ajouts purs, ne modifie
# aucune RPC existante. Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-27-flexible-commercial-documents.sql
# Espace commercial flexible (feature/flexible-commercial-documents) :
# resserre client_accept_devis/client_refuse_devis (rejette une version de
# devis remplacée par une révision plus récente, devis.supersededByQuoteId)
# + étend get_my_client_devis_detail/get_my_client_devis/
# get_my_client_facture_detail (allowlist additive : parentQuoteId/
# revisionNumber/supersededByQuoteId/documentSnapshot, absents avant ce
# chantier). Seule extension serveur du chantier -- numérotation/snapshot/
# révisions/modèles de document vivent entièrement dans seba_state.

migrations/2026-07-26-public-intake.sql
# Fondation acquisition (feature/public-intake-conversion) : table dédiée
# public_service_requests (RLS select/update patron uniquement, aucun accès
# anon/authenticated en écriture directe -- l'insertion publique passe par
# l'Edge Function public-intake, service_role) + 2 RPC patron
# (convert_public_service_request résout/crée le client uniquement,
# link_public_service_request_conversion pose ensuite converted_quote_id/
# converted_intervention_id une fois le devis/l'intervention créés côté
# navigateur par les moteurs JS existants). Ajouts purs, ne modifie aucune
# RPC/table existante. Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-28-account-activation-bootstrap.sql
# Activation réelle des comptes et accès (prérequis avant déploiement des
# emails commerciaux) : create_profile_and_company crée désormais aussi la
# ligne seba_state du patron à l'activation (sinon aucune ligne n'existe
# avant le premier sync-push, qui EXIGE une ligne seba_state existante
# pour résoudre l'identité -- un patron neuf ne pouvait rien écrire).
# CREATE OR REPLACE rejoué APRÈS le durcissement T2
# (migrations/2026-07-22-fix-t2-onboarding-sector-idempotence.sql) : repart
# du corps durci de T2 (SECURITY DEFINER, search_path, validation secteur,
# idempotence profiles/companies) tel quel, ajoute uniquement le bootstrap
# seba_state -- aucune régression du durcissement existant, ne modifie
# aucune autre RPC/table. Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-28-sync-push-state-persistence.sql
# QA360-P0-A : termine le Palier 1 (sync-push, PR #32/2454af4, 2026-07-09) --
# apply_entity_patch() écrivait uniquement dans entity_versions, jamais
# dans seba_state.state (lu par SupabaseAdapter.pull() et la quasi-totalité
# des RPC de lecture) : déployer sync-push tel quel aurait donné un succès
# HTTP sans persistance réelle après reload. Remplace apply_entity_patch()
# (nouvelle signature à 5 paramètres, ajout de p_op create/update/delete,
# ancienne signature à 4 paramètres supprimée -- aucun autre appelant dans
# le dépôt) pour écrire seba_state.state ET entity_versions atomiquement
# dans la même transaction, verrouillage par ligne seba_state (par compte).
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-28-sync-push-service-role-execute.sql
# Rend explicite le privilège EXECUTE de service_role sur
# apply_entity_patch(text,text,text,jsonb,text) -- déjà présent en
# pratique via les privilèges par défaut de la plateforme Supabase
# (vérifié : has_function_privilege + 550 appels réels réussis pendant
# les tests de la migration précédente, aucun échec de permission),
# rendu explicite ici plutôt que dépendant d'une convention implicite.
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-29-update-my-entreprise.sql
# THEME-MOBILE-001 : SebaDB.entreprise.set() (docs/seba-data.js) n'a jamais
# envoyé la moindre requête réseau -- persist() n'écrit que le cache local
# depuis le Palier 1, et pushOp()/sync-push ne couvrent que les collections
# tableau (entreprise est un objet unique). Nouvelle RPC dédiée, écriture
# authentifiée directe (SECURITY INVOKER, RLS state_update existante),
# même emplacement canonique que le bootstrap (create_profile_and_company).
# Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-29-settings-brand-identity.sql
# SETTINGS-BRAND-001 : étend l'allowlist de update_my_entreprise() avec
# "branding" ({accent, logoUrl}) et crée le bucket Storage public
# "company-logos" (policies scoped par auth.uid(), pas de helper
# SECURITY DEFINER nécessaire -- le propriétaire d'un chemin EST
# directement auth.uid()). Voir l'en-tête du fichier pour le détail complet.

migrations/2026-07-29-public-intake-config-persistence.sql
# SETTINGS-BRAND-001 : SebaDB.publicIntake.setConfig() (docs/seba-data.js)
# n'a jamais envoyé la moindre requête réseau -- même défaut que
# entreprise.set() avant son propre correctif (objet unique, hors du
# contrat pushOp()/sync-push). Conséquence réelle : l'Edge Function
# public-intake lit seba_state.state.publicIntakeConfig en base et le
# trouvait TOUJOURS null -- le formulaire public restait bloqué
# "désactivé" quoi que le patron configure côté navigateur. Nouvelle RPC
# dédiée, même schéma que update_my_entreprise. Voir l'en-tête du fichier
# pour le détail complet.

migrations/2026-08-02-portal-notifications-foundation.sql
# Lot 1 du programme d'amélioration des portails Client/Salarié : table
# dédiée `notifications` + `notification_preferences` (RLS stricte, accès
# exclusivement via RPC security definer), trigger AFTER INSERT sur
# seba_messages, et 3 RPC existantes (client_accept_devis/
# client_refuse_devis/complete_my_intervention) étendues de façon
# additive (CREATE OR REPLACE reproduisant leur logique existante à
# l'identique + un appel de notification avant le retour de succès). Voir
# l'en-tête du fichier pour le détail complet.

# ══════════════════════════════════════════════════════════════════
# OVERLAY FACULTATIF (jamais appliqué par défaut — commande explicite uniquement)
# ══════════════════════════════════════════════════════════════════
[OVERLAY-RGPD]
migrations/2026-07-11-rgpd-suppression-compte.sql
# Statut : OVERLAY MANUEL FACULTATIF.
# Raison : cette migration porte son propre en-tête "À exécuter dans l'éditeur
# SQL Supabase (action fondateur)" -- conçue dès l'origine comme une action
# manuelle ponctuelle, jamais fusionnée dans supabase-schema.sql (confirmé :
# aucune trace de "erase_account_completely"/"rgpd" dans le baseline, grep
# insensible à la casse, zéro résultat). Ne doit donc jamais s'appliquer
# implicitement lors d'une reconstruction standard.

# ══════════════════════════════════════════════════════════════════
# HISTORIQUE ABSORBÉ — documentation uniquement, NE JAMAIS REJOUER
# Chacune des 9 lignes suivantes crée un objet DÉJÀ présent dans le baseline
# actuel de supabase-schema.sql. Les rejouer après le baseline provoque au
# mieux un no-op silencieux, au pire une erreur (cas réel rencontré :
# renommage de paramètre RPC, voir 20260720b ci-dessous).
# ══════════════════════════════════════════════════════════════════
# migrations/20260709_create_client_memoire.sql
#   Objet absorbé   : vue `client_memoire`
#   Preuve           : supabase-schema.sql:861 (commentaire renvoie explicitement à ce fichier)
#   Raison non-exec  : vue déjà créée dans le baseline, recréation redondante

# migrations/20260709_create_api_usage_guardrail.sql
#   Objet absorbé   : table `api_usage_daily`
#   Preuve           : supabase-schema.sql:905
#   Raison non-exec  : table déjà créée dans le baseline

# migrations/20260716_create_seba_messages.sql
#   Objet absorbé   : table `seba_messages`
#   Preuve           : supabase-schema.sql (table présente) ; fichier s'auto-déclare
#                      "Idempotent : peut être rejoué sans risque. Voir supabase-schema.sql" (ligne 2)
#   Raison non-exec  : déjà dans le baseline, ré-exécution documentée comme inutile par l'auteur lui-même

# migrations/20260719_client_espace.sql
#   Objet absorbé   : tables `client_accounts`, `client_requests`
#   Preuve           : supabase-schema.sql:999, 1012 ; fichier s'auto-déclare
#                      "Idempotent : peut être rejoué sans risque. Voir supabase-schema.sql" (ligne 4)
#   Raison non-exec  : déjà dans le baseline

# migrations/20260719c_universal_auth_employe.sql
#   Objet absorbé   : table `employe_accounts`
#   Preuve           : supabase-schema.sql:329
#   Raison non-exec  : déjà dans le baseline

# migrations/20260719d_client_requests_intervention_link.sql
#   Objet absorbé   : colonne `intervention_id` sur `client_requests`
#   Preuve           : supabase-schema.sql:1020, intégrée directement dans la définition de table
#                      (pas un `alter table` séparé -- le baseline reflète déjà l'état final fusionné)
#   Raison non-exec  : colonne déjà présente dans la définition de table du baseline

# migrations/20260720_mission_chat.sql
#   Objet absorbé   : colonne `request_id` sur `seba_messages`
#   Preuve           : supabase-schema.sql:1067
#   Raison non-exec  : déjà dans le baseline

# migrations/20260720b_close_mission_rpc.sql
#   Objet absorbé   : fonction `close_my_intervention` -- MAIS sous une forme SUPERSÉDÉE
#                      (paramètre nommé `_photo_name`, remplacé ensuite par `_photo_path`)
#   Preuve           : ce fichier définit `_photo_name` (ligne 20) ; le baseline a `_photo_path`
#                      (supabase-schema.sql:1236) -- la version du baseline vient de 20260720c, pas de ce fichier
#   Raison non-exec  : rejouer ce fichier après le baseline ÉCHOUE (PostgreSQL refuse de renommer un
#                      paramètre via CREATE OR REPLACE) -- confirmé empiriquement le 2026-07-22

# migrations/20260720c_mission_photos_storage.sql
#   Objet absorbé   : fonction `close_my_intervention(..., _photo_path)`, bucket `mission-photos`
#   Preuve           : supabase-schema.sql:1236 (fonction, forme finale identique), :1309 (bucket)
#   Raison non-exec  : déjà dans le baseline, sous sa forme finale exacte

# ══════════════════════════════════════════════════════════════════
# SCRIPT DE TEST — jamais un objet de schéma, jamais appliqué au schéma
# ══════════════════════════════════════════════════════════════════
# migrations/20260709_create_client_memoire.test.sql
#   Nature : script de vérification RLS manuel (son propre en-tête le dit explicitement :
#            conçu pour être collé dans Supabase SQL Editor après déploiement de client_memoire).
#   Traitement : réutilisé tel quel comme test dans scripts/local-db/verify.sh, JAMAIS
#            listé comme migration de schéma à appliquer.
