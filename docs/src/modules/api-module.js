/**
 * @module modules/api-module
 * Squelette — pas encore implemente. Doit rester le seul module autorise
 * a appeler window.sebaAuth.rpc(...), en validant le contenu recu via
 * PROPULSION_SAVE_REQUESTED avant tout appel reseau. Voir
 * ARCHITECTURE-MODULAIRE.md section C pour la strategie de securite
 * prevue (la protection reelle reste les policies RLS cote serveur).
 */
export default {};
