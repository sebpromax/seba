# Seba — Master Backlog

Dernière mise à jour : 2026-07-29
Branche observée : main
Commit observé : 14e2ba20

## Règle de fonctionnement

- ce fichier est la source de vérité unique des tâches restantes du projet Seba ;
- une tâche doit être ajoutée ici dès qu'elle est décidée ;
- une tâche passe à EN COURS au démarrage ;
- une tâche est retirée des sections actives après livraison prouvée (commit, PR, test ou validation externe) ;
- aucune tâche ne peut être marquée terminée sans preuve ;
- aucune nouvelle checklist concurrente ne doit être créée (voir `_architecture/DEPLOYMENT_CHECKLIST.md` et `PLAN.md`, dont les tâches actives ont été centralisées ici).

### Filtre de valeur concrète

- aucune tâche ne passe à `EN COURS` sans bénéficiaire identifié ;
- aucune tâche ne passe à `EN COURS` sans résultat observable ;
- aucune tâche technique ne devient prioritaire sans blocage ou risque réel ;
- les idées non validées restent `À DÉCIDER` ou `REPORTÉES` ;
- une preuve locale ne vaut pas automatiquement une validation en production ;
- après une fondation technique, priorité au produit ou au pilote ;
- le nombre de tâches terminées ne mesure pas le progrès ;
- le progrès est mesuré par les parcours réellement utilisables.

> Seba avance lorsqu'un professionnel peut accomplir davantage de travail réel avec moins de friction, pas lorsque le dépôt contient davantage de complexité.

Priorités (inchangées) :

- `P0` : bloque réellement l'usage, les données, la sécurité ou la production ;
- `P1` : amélioration directement nécessaire au parcours actif ;
- `P2` : utile après validation du besoin ;
- `P3` : reporté ou expérimental.

## Ordre recommandé (priorités de livraison)

1. Correctif critique d'activation sur `main` (AUTH-006).
2. Migration `account-activation-bootstrap` distante (AUTH-007).
3. Contrôle CI design system (CORE-008).
4. Déployer `sync-push` en production, le seul verrou qui empêche toute écriture métier réelle de persister (QA360-P0-A).
5. Diagnostiquer l'échec d'envoi d'invitation indépendamment de l'achat de domaine (QA360-P0-B).
6. Achat du domaine et activation Resend (CED-004).
7. Validation réelle des emails (CED-008).
8. Validation terrain du pilote (PILOT-001).
9. Automatisations seulement après l'envoi manuel réel (AUTO-001).

## À terminer maintenant

| ID | Priorité | Domaine | Tâche | Statut | Dépendance ou blocage | Prochaine action exacte | Preuve/source |
|---|---|---|---|---|---|---|---|
| QA360-P0-B | P0 | Customer Email Delivery | **Cause confirmée 2026-07-30** (pas seulement reconfirmée en symptôme) : test isolé direct sur `send-email` (script `scratchpad/qa/test-send-email-resend.mjs`, identifiants saisis localement par le fondateur, jamais transmis) — Resend répond `200` et crée un message, mais le statut de livraison réel est `failed`, événement exact *"Domain is not verified: The domain used to send this email needs to be verified."* L'expéditeur `onboarding@resend.dev` (domaine partagé Resend, jamais vérifié) est la cause. `client-provision`/`employe-provision`/signup partagent un lien de code confirmé (même relais SMTP Supabase Auth, même config Resend documentée) mais leur panne exacte n'a pas été reconfirmée par un test isolé équivalent — probable mais pas certaine à 100 %. Correctif minimal déjà préparé (`supabase-functions/send-email.ts`/`daily-digest.ts` : `RESEND_FROM` obligatoire, plus de repli codé en dur) — pas encore déployé | À FAIRE | Le fondateur doit vérifier un domaine réel dans Resend (Domains → Add Domain), configurer `RESEND_FROM` + le même expéditeur dans Supabase Auth SMTP Settings | Suivre l'ordre exact de `_architecture/QA360_P0_REMEDIATION_PLAN.md` section 6 (vérification domaine → secrets → redéploiement → 4 tests de confirmation) | Reproduit le 2026-07-22 via `POST /auth/v1/invite` ; cause confirmée le 2026-07-30 via test direct Resend, détail complet dans `_architecture/QA360_P0_REMEDIATION_PLAN.md` |
| QA360-P2-A | P2 | Portail client | Bannière "Action prioritaire" de `docs/client-espace.html` (lignes 559/561) affiche 0€ pour un devis/facture dont le montant est stocké sous `.totalTTC` — il manque le fallback `d.totalTTC != null ? d.totalTTC : d.amount` déjà présent ailleurs dans le même fichier (lignes 727, 848) | À FAIRE | Aucun | Appliquer le même fallback aux lignes 559/561 | Constat QA360-A02-003, confirmé par lecture directe du code (incohérence entre 4 emplacements du même fichier) |
| QA360-P2-B | P2 | Portail employé | Le détail d'une mission côté employé (`espace-terrain.html`) n'affiche pas l'adresse du client, alors que la liste du jour l'affiche déjà. Cause probable : le détail est rendu depuis l'objet de liste déjà chargé (`get_my_employee_interventions()`, qui ne fait pas la jointure client) plutôt qu'en rappelant le RPC dédié à un détail enrichi — la même jointure existe déjà dans le code SQL pour `get_my_client_intervention_detail()` (`migrations/2026-07-25-intervention-360.sql:89-93`, commentaire explicite : "sans ça, le détail d'une mission perdrait l'adresse") | À FAIRE | Aucun | Vérifier si `openMissionDetail()` (espace-terrain.html) réutilise l'objet de liste ou rappelle un RPC de détail enrichi ; si c'est le premier cas, le faire rappeler le détail enrichi | Constat QA360-A02-012, cause racine non encore vérifiée dans le JS (hypothèse basée sur le code SQL) |
| QA360-P3-A | P3 | UI | Le texte "Compte de démonstration" reste codé en dur dans `sidebar.js` sans condition sur l'état réel de la session (fichier à rayon d'impact large, plusieurs pages) | À FAIRE | Aucun | Conditionner ce texte à un vrai indicateur de mode démo, pas un texte fixe | Constat QA360-A01-005 |
| PILOT-004 | P0 | Pilote produit | Premier cycle métier réel en production : connexion patron → configuration entreprise → création client → devis → consultation/acceptation client → intervention → planning → assignation → exécution mobile → compte rendu/preuve → approbation client → validation patron → facture → paiement → historique client complet (15 étapes). **Audit de vérité du 2026-07-30** (`SEBA_PILOT_READINESS_AUDIT.md`) confirme : les étapes patron seul (clients, interventions, devis y compris révision d'un devis signé, factures, paiements y compris partiels) fonctionnent réellement et persistent (vérifié par requêtes serveur directes). Mais les étapes qui nécessitent un patron nouveau OU un employé/client invité sont bloquées par QA360-P0-B (email cassé) — verdict **NO-GO** sur le cycle complet tant que non résolu | BLOQUÉ | QA360-P0-B (panne email/SMTP Supabase) | Corriger QA360-P0-B en premier ; puis reprendre le cycle 15 étapes avec un vrai compte employé + client créés via une invitation qui fonctionne (pas de nouveaux comptes de test tant que l'email est cassé) | `_architecture/PILOT_RUN_001.md`, `_architecture/SEBA_PILOT_READINESS_AUDIT.md`, `_architecture/SEBA_EXECUTABLE_CAPABILITY_MATRIX.md` |
| LANDING-HOMEPAGE-001 | P2 | Marque / Home publique | Refonte de la home publique (`docs/index.html`), pilotée par le fondateur comme exercice multi-agents (7 agents + 3 concepts créatifs Conservateur/Premium/Radical + arbitrage). Phase 1 (définition de marque) terminée : `SEBA_HOMEPAGE_BRAND_VISION.md` puis `SEBA_BRAND_CHARTER.md` (Version 1.0, validée par le fondateur 2026-07-29) — noyau de marque, cible, promesse, vocabulaire, ton, principes de confiance et philosophie produit fixés. Direction retenue : Concept Premium + continuité d'un même client à travers les 4 captures produit réelles ; signatures graphiques "coin corné" et "chanfrein" explicitement rejetées, reconnaissance de marque via la discipline d'exécution | EN COURS | Aucun (charte poussée sur `main`) | Produire le plan de design concret de la home (wireframe section par section, copy complète, composition desktop/mobile, palette et typographie définitives, règles d'usage des captures, interactions/animations, fichiers à modifier, stratégie de réalisation progressive, critères de validation) — aucun code avant validation de ce plan par le fondateur | `_architecture/SEBA_BRAND_CHARTER.md`, `_architecture/SEBA_HOMEPAGE_BRAND_VISION.md` |

**Écart assumé (résolu)** : il avait été dit que PILOT-004 "reprend immédiatement après la consolidation" de QA360-001 ; il a été passé `BLOQUÉ` à la place, le temps de corriger et déployer réellement `sync-push` (voir archive QA360-P0-A). Résolu le 2026-07-28 : `sync-push` déployée et validée en production réelle, PILOT-004 repasse `EN COURS`.

Écart assumé (résolu) : THEME-MOBILE-001 et SETTINGS-BRAND-001 étaient temporairement tous deux `EN COURS` (doctrine "un seul chantier `EN COURS`", `CLAUDE.md`), le second explicitement commandé par le fondateur avant l'archivage du premier. Résolu le 2026-07-29 : SETTINGS-BRAND-001 validé en production sur un compte patron réel (8/8 confirmés) et archivé ; QA360-P1-A repris en parallèle, corrigé, testé, déployé et archivé le même jour ; THEME-MOBILE-001 validé physiquement sur iPhone réel par le fondateur (mode clair, mode sombre, sidebar mobile, premier/dernier élément, scroll complet, Assistant sans chevauchement, aucun espace blanc anormal, portrait, paysage — 10/10 confirmés) et archivé. **PILOT-004 repasse `EN COURS`** (sa seule dépendance — l'archivage de THEME-MOBILE-001 — est résolue) : premier chantier produit actif restant côté pilote.

**Écart assumé (assumé, non résolu)** : LANDING-HOMEPAGE-001 est également `EN COURS` en parallèle de PILOT-004, en violation littérale de la doctrine "un seul chantier `EN COURS`" (`CLAUDE.md`). Ceci est un chantier de marque/marketing explicitement piloté et commandé par le fondateur lui-même (exercice multi-agents, arbitrage direct), distinct du chantier produit PILOT-004 — même précédent que THEME-MOBILE-001/SETTINGS-BRAND-001 ci-dessus. Documenté explicitement plutôt que silencieusement ignoré.

**2026-07-30 — Le fondateur a explicitement mis en pause le chantier visuel** (LANDING-HOMEPAGE-001, migration du système de couleur Compagnon en cours) après avoir rejeté un premier audit terrain hostile (42 scénarios) comme verdict produit définitif — cet audit avait surtout testé le site public GitHub Pages sans distinguer panne d'infrastructure, absence de fonctionnalité et bug applicatif. Il a ordonné un audit de vérité (Phase 0) : cartographie de l'environnement réel, matrice de capacités, vérification des invariants métier, puis seulement 12 tests de lancement ciblés (pas les 42 scénarios). Résultat : `_architecture/SEBA_EXECUTABLE_CAPABILITY_MATRIX.md` + `_architecture/SEBA_PILOT_READINESS_AUDIT.md`, verdict **NO-GO** sur PILOT-004 (voir ligne PILOT-004 et QA360-P0-B ci-dessus) à cause d'une panne email/SMTP bloquant toute nouvelle inscription et toute invitation employé/client — pas à cause du cœur métier, qui fonctionne réellement. LANDING-HOMEPAGE-001 reste `EN COURS` mais en pause, à reprendre après résolution de QA360-P0-B.

## Bloqué par une action externe

| ID | Priorité | Domaine | Tâche | Statut | Dépendance ou blocage | Prochaine action exacte | Preuve/source |
|---|---|---|---|---|---|---|---|
| CED-004 | P0 | Customer Email Delivery | Acheter un nom de domaine Seba, l'ajouter dans Resend, configurer SPF/DKIM, attendre la validation — bloque aussi bien les emails commerciaux que l'inscription par email réel d'un vrai patron (AUTH-006/AUTH-007 valident le mécanisme RPC/RLS, pas la délivrabilité de l'email lui-même, toujours en sandbox Resend) | BLOQUÉ | Action fondateur (achat + attente DNS) | Achat du domaine par le fondateur | `_architecture/DEPLOYMENT_CHECKLIST.md` ; `MANUEL-SEBA-ADMIN.md` ligne 14 (sandbox Resend = même cause racine) |
| CED-006 | P1 | Customer Email Delivery | Configurer les secrets distants `RESEND_API_KEY`/`EMAIL_FROM`/`APP_BASE_URL`/`RESEND_WEBHOOK_SECRET` (noms seulement, jamais les valeurs ici) | BLOQUÉ | CED-004 | Une fois le domaine validé, saisir les secrets dans Supabase → Edge Functions → Secrets | `_architecture/DEPLOYMENT_CHECKLIST.md` |
| CED-007 | P1 | Customer Email Delivery | Config Resend complète : domaine expéditeur validé, webhook configuré, événements limités aux événements gérés, secret webhook, test de signature réelle | BLOQUÉ | CED-004, CED-006 | Configurer le webhook Resend une fois le domaine validé, tester une signature réelle | `supabase/functions/commercial-email-webhook/index.ts` |
| CED-008 | P1 | Customer Email Delivery | Validation réelle en production : invitations (patron/client/employé), reset (patron/client/employé), envoi devis/facture/reçu, deep-link post-auth, isolation client A/B, idempotence, statuts sent/delivered/failed, zéro secret exposé | BLOQUÉ | CED-004, CED-006, CED-007 | Rejouer `scripts/qa-account-activation.js` + les scénarios d'envoi contre le projet distant une fois le domaine validé | `scripts/qa-customer-email-delivery-phase1.js`, `scripts/qa-customer-email-delivery-phase2.js` (équivalents locaux déjà écrits) |
| AUTH-005 | P2 | Authentification | Statut de livraison réel + bouton "Réessayer l'envoi" pour les invitations client/employé (`generateLink()` + envoi direct via l'API Resend au lieu de `auth.admin.inviteUserByEmail()`, table `invitation_log`, UI `client-fiche.html`/`employe-fiche.html`) — comportement utile déjà écrit sur `fix/invitation-delivery` (commit `7193874`), non porté : dépend de `RESEND_API_KEY` (même blocage externe que CED-004/CED-006) et casserait Mailpit en local tant que ce secret n'existe pas | BLOQUÉ | CED-004, CED-006 | Une fois CED-004/CED-006 résolus, réévaluer le portage de `supabase-functions/_shared/invitation-delivery.ts` + `migrations/2026-07-23-invitation-delivery-log.sql` sur `feature/customer-email-delivery`, en vérifiant `scripts/qa-account-activation.js` derrière | Branche `fix/invitation-delivery` (diff isolé via `git diff 63fb0d9 fix/invitation-delivery`) |
| RGPD-001 | P1 | RGPD | Aucune fermeture/effacement en self-service pour un compte client/employé invité (`erase_account_completely` ne couvre que le patron) | À DÉCIDER | Décision fondateur sur 9 points avant tout code (fermeture accès / effacement personnel / anonymisation métier / conservation légale factures / messages / Storage / multi-compte / dual-rôle / traçabilité) | Soumettre le design révisé (pas un simple strip JSONB, déjà rejeté) au fondateur pour arbitrage point par point | Mémoire `project_rgpd_client_employee_deletion_debt` (rejet explicite 2026-07-22) |
| LEGAL-001 | P2 | Juridique | Avis juridique sur la responsabilité de la plateforme en cas de dommage causé par un professionnel "vérifié" | BLOQUÉ | Juriste non assigné | Engager un juriste | `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 15 |
| LEGAL-002 | P2 | Juridique | Avis RGPD/CNIL sur les nouveaux flux publics (téléphone visiteur, fiche publique, liste d'attente) + cookies + rédaction CGV | BLOQUÉ | Juriste RGPD non assigné | Engager un prestataire RGPD/juriste | `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 16 ; `AUDIT-EXPERT.md` §8 |
| LEGAL-003 | P1 | Juridique/Comptable | Vérification de conformité facturation France avant toute facturation réelle émise via Seba | BLOQUÉ | Expert-comptable non assigné | Engager un expert-comptable | `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 14 |
| LEGAL-004 | P2 | Juridique | `docs/politique-confidentialite.html` contient toujours le placeholder `[À compléter par le fondateur : raison sociale, forme juridique, SIREN, adresse du siège]` | BLOQUÉ | Fondateur (identité légale réelle) | Le fondateur fournit raison sociale/SIREN/adresse | `docs/politique-confidentialite.html:72` (vérifié présent) |
| PILOT-001 | P1 | Pilote | GATE-0 — validation terrain (10 professionnels nettoyage/conciergerie + 5 acteurs demande, zone Cap-d'Ail/Beausoleil/Roquebrune/Menton) jamais réalisée ; bloque tout le Groupe 3/4/5 | BLOQUÉ | Action fondateur (entretiens réels) | Le fondateur conduit les entretiens, rédige la synthèse, enregistre GO/AJUSTER/STOP dans `SEBA_DECISION_LOG.md` | `_architecture/SEBA_EXECUTION_ROADMAP.md` §GATE 0 ; `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 1 |

## À faire ensuite

| ID | Priorité | Domaine | Tâche | Statut | Dépendance ou blocage | Prochaine action exacte | Preuve/source |
|---|---|---|---|---|---|---|---|
| CORE-001 | P2 | Socle produit | Périmètre P5 (analytique financière) incomplet : tables `intervention_materiaux`/`intervention_trajets`/`fournisseurs_prix_historique`, table `client_payment_history` + prédiction impayés, widget dashboard "marge réelle" | À FAIRE | Aucune | Concevoir le schéma des 3 tables restantes, puis le widget consommant `vue_marge_interventions`/`get_marge_reelle` | `PLAN.md` lignes 34-36 |
| CORE-004 | P2 | Socle produit | Aucune métrique de monitoring serveur : échecs de synchro, latence `vision-qa`, backlog d'alertes ne remontent qu'en `console.warn` local, jamais au patron/admin | À FAIRE | Aucune | Construire au minimum la métrique "sync failures" + latence vision-qa | `PLAN.md` ligne 69 ; `AUDIT-GO-LIVE-SEBA.md` ligne 131 |
| PILOT-002 | P2 | Pilote | Groupe 1bis — formulaire de liste d'attente publique (finalité, données, consentement, conservation, anti-spam, notification fondateur) | À FAIRE | Doit rester sans retarder aucune tâche active | Définir finalité/données strictement nécessaires, puis construire le formulaire | `_architecture/SEBA_EXECUTION_ROADMAP.md` §Groupe 1bis |
| DASH-001 | P2 | Dashboard | Décision bloquante en attente avant la Phase 1 du Dashboard V2 Master Plan (non précisée plus avant dans ce backlog pour éviter la duplication du plan complet) | À DÉCIDER | Arbitrage fondateur/produit | Lire `DASHBOARD_V2_MASTER_PLAN.md` §"Décision en attente" et trancher | `_architecture/DASHBOARD_V2_MASTER_PLAN.md` ligne 25 |
| DEPLOY-DRIFT-001 | P1 | Déploiement | Réconcilier les Edge Functions déployées avec les sources versionnées : `client-provision`/`employe-provision` sont `ACTIVE` sur `ptmudezhxnhhyctowlqp` mais n'existent dans aucun commit de l'historique git — le déployé et le versionné ont divergé | À FAIRE | Aucun (ne pas commencer maintenant, hors périmètre QA360-P0-A) | Comparer le code réellement déployé (à récupérer via le dashboard Supabase ou en redemandant confirmation du contenu) au code présent sur disque, committer la version qui correspond exactement à ce qui tourne en production | Constat QA360-A01/A02, confirmé par `git log --all -- supabase/functions/client-provision supabase/functions/employe-provision` (vide) |

## À décider

| ID | Priorité | Domaine | Tâche | Statut | Dépendance ou blocage | Prochaine action exacte | Preuve/source |
|---|---|---|---|---|---|---|---|
| CORE-006 | P2 | RGPD/Socle | Tension non tranchée entre append-only (`sync_operations`) et droit à l'effacement Art. 17 — `eraseAllData()` ne purge pas `sync_operations`/`employe_sessions`/`employe_credentials` côté serveur | À DÉCIDER | Probable anonymisation plutôt que suppression physique — non tranché | Faire trancher par le fondateur (avec avis juridique si nécessaire, voir LEGAL-002) | `PLAN.md` ligne 72 |
| CORE-007 | P3 | Socle produit | Sort des scripts `verify-*`/`preview-*` potentiellement obsolètes (inventaire fait) | À DÉCIDER | Autorisation d'archivage non donnée | Le fondateur autorise l'archivage ou la suppression | `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 4 ("Inventaire fait — décision d'archivage en attente d'autorisation") |
| QA-002 | P3 | QA | Dérive de date/heure des baselines visuelles du dashboard (expirent à minuit et selon le message d'accueil horaire) — fix par gel d'horloge proposé, jamais autorisé | À DÉCIDER | Autorisation fondateur | Le fondateur valide (ou non) le gel d'horloge dans `qa-dashboard-full.js`/`qa-visual-regression.js` | Mémoire `project_qa_baseline_date_drift` |
| PUBLIC-CARD-001 | P2 | Carte publique / QR code | **Non construit — vision produit à conserver, aucun onglet/bouton créé dans l'interface.** Futur onglet Réglages permettant au patron de créer une carte publique de son entreprise : logo, nom, couleur de marque, description, coordonnées, zone d'intervention, prestations, tarifs choisis, image/carte tarifaire déjà existante, bouton de contact, lien de demande en ligne, QR code permanent, téléchargement du QR code, page publique mobile partageable. Le QR code pointera vers une URL publique stable appartenant à Seba. Doit réutiliser l'identité canonique (nom/logo/couleur, désormais réelle et validée en production depuis SETTINGS-BRAND-001), les services et les demandes en ligne existants, jamais une nouvelle source de données | À DÉCIDER | Dépendance résolue (SETTINGS-BRAND-001 validé en production 2026-07-29) — reste néanmoins à décider : aucune autorisation explicite de démarrage donnée | Le fondateur autorise le démarrage ; puis concevoir le schéma minimal de la carte publique (probablement un sous-objet de `entreprise`, à trancher) et le générateur de QR code | Demande fondateur, section 11 de la mission SETTINGS-BRAND-001 |

## Reporté

| ID | Priorité | Domaine | Tâche | Statut | Dépendance ou blocage | Prochaine action exacte | Preuve/source |
|---|---|---|---|---|---|---|---|
| CORE-002 | P3 | Socle produit | Aucun environnement CI/CLI Deno n'a jamais exécuté réellement la suite de tests unitaires (`*.test.ts` : sync-push, vision-qa, conscience-seba, llm-providers, invitation-delivery…) | REPORTÉ | Les tests Deno en CI restent utiles, mais aucun échec Deno réel ne bloque actuellement un utilisateur ou le pilote. Cette fondation ne doit pas retarder la validation concrète du cycle métier. | Redevient prioritaire si : régression réelle d'une Edge Function ; tests Deno nécessaires avant un déploiement critique ; plusieurs développeurs interviennent simultanément sur le backend ; le pilote dépend d'une fonction insuffisamment sécurisée par les tests actuels | `PLAN.md` ligne 53 |
| AUTO-001 | P3 | Automatisations email | `send_quote_email`/`send_invoice_email`/`send_receipt_email`, intégration builder, idempotence des exécutions automatiques, QA final | REPORTÉ | L'envoi manuel réel doit d'abord être validé en production (CED-008) | Reprendre après validation réelle de CED | Instruction explicite de ce chantier (2026-07-28) |
| PILOT-003 | P3 | Pilote | Groupes 3 à 6 — face publique complète (fiche publique, revendication, demandes qualifiées, pilote restreint) | REPORTÉ | PILOT-001 (GATE-0) non résolu | Ne démarre qu'après décision GO/AJUSTER enregistrée dans `SEBA_DECISION_LOG.md` | `_architecture/SEBA_EXECUTION_ROADMAP.md` Groupes 3-6 |
| CORE-003 | P3 | Socle produit | Pas de killswitch DB pour `vision-qa.ts`/`sync-push.ts` (seul le trigger d'alerte a un vrai killswitch) — proposition `app_config(key,value)` déjà écrite, jamais créée | REPORTÉ | Non prioritaire | Créer `app_config` quand un vrai besoin d'arrêt d'urgence se présente | `PLAN.md` ligne 68 (vérifié : table `app_config` absente du schéma) |
| CORE-005 | P3 | Sécurité | Comparaison à temps constant manquante pour le secret dans `notify-alert.ts` | REPORTÉ | Sévérité faible, non bloquant | Remplacer la comparaison directe par une comparaison à temps constant | `PLAN.md` ligne 70 |
| QA-001 | P3 | QA | `AUDIT-GO-LIVE-SEBA.md`/`AUDIT-EXPERT.md` contiennent des cases `- [ ]` obsolètes pour du travail déjà livré (vérifié : `revoke execute`/`AbortSignal.timeout`/`robots.txt`+`sitemap.xml` déjà présents dans le code) | REPORTÉ | Hygiène documentaire, non bloquant | Cocher ou archiver ces 2 rapports lors d'un prochain passage doc | Vérification directe : `supabase-schema.sql` (7×`revoke execute`), `supabase-functions/vision-qa.ts:111`, `docs/robots.txt`+`docs/sitemap.xml` présents |
| LEGAL-005 | P3 | Juridique | Vérification juridique/fiscale/opérationnelle d'une ouverture à Monaco | REPORTÉ | Juriste droit du travail/fiscalité transfrontalière, non urgent | Aucune — explicitement reporté | `_architecture/SEBA_OWNERS_AND_DEADLINES.md` ligne 17 |

## Archive récente

| ID | Tâche terminée | Preuve |
|---|---|---|
| THEME-MOBILE-001 | Interface mobile/thèmes du dashboard patron, de la navigation et de l'Assistant Seba entièrement corrigée sur 3 vagues de correctifs (PR #100/#101/#102) : Assistant tokenisé et masqué correctement derrière la sidebar mobile, thème 100% `prefers-color-scheme` (aucun toggle manuel), sidebar redimensionnée et theme-aware sur toutes les pages, canevas `html`/`body` réellement peint (cause racine du texte invisible + "grand espace vide" en thème sombre), sidebar mobile avec safe-area iOS (`env(safe-area-inset-top/bottom)`) et `justify-content:safe center`. 155 assertions Playwright (webkit+chromium) + revue indépendante (2e passe from-scratch). **Validation physique réelle sur iPhone confirmée par le fondateur (2026-07-29) : mode clair, mode sombre, sidebar mobile, premier élément visible, dernier élément accessible, scroll complet, Assistant sans chevauchement, aucun espace blanc anormal, portrait, paysage — 10/10 confirmés OK** | PR #100 (commit `31b764b`), PR #101 (commit `82756db`), PR #102 (commit `0179259`), `scripts/qa-mobile-theme-layout.js` + `scripts/qa-mobile-system-theme-empty-state.js` (155/155), validation iPhone réelle fondateur 2026-07-29 |
| SETTINGS-BRAND-001 | Réglages réels et identité de marque — sidebar mobile (safe-area iOS + `justify-content:safe center`), sidebar figée en noir corrigée sur 8 pages (règle CSS locale morte écrasant `pro-global.css`), "Supprimer mon entreprise" restreint à l'onglet Compte, avatar dynamique (`SebaDB.entreprise.initials()`/`renderAvatar()`, "ML"/"PA" codés en dur supprimés sur 14 pages + dashboard.html + historique.html), couleur de marque et logo d'entreprise réellement sauvegardés (`branding:{accent,logoUrl}` sur `entreprise`, bucket Storage public `company-logos`, script partagé `docs/brand-accent.js`), configuration "Demande en ligne" réellement persistée (nouvelle RPC `update_my_public_intake_config`, l'Edge Function `public-intake` voyait `cfg=null` en permanence avant ce correctif). PR #104 mergée et déployée, 2 migrations distantes appliquées sur `ptmudezhxnhhyctowlqp` (confirmé fondateur), vérifiées structurellement (RPC/bucket existent réellement). **Validation manuelle réelle en production sur un compte patron réel (2026-07-29, confirmée fondateur) : couleur de marque, persistance après reload, persistance après reconnexion, logo, initiales dynamiques, suppression de l'ancien "ML", configuration Demande en ligne, page publique mise à jour — 8/8 confirmés OK** | PR #104 (commits `790e3e1`/`fe87aaa`, mergé `e5e5c159`), migrations `2026-07-29-settings-brand-identity.sql` + `2026-07-29-public-intake-config-persistence.sql`, `scripts/qa-settings-brand-identity.js` (60/60), validation production manuelle fondateur 2026-07-29 |
| QA360-P1-A | `app/dashboard.html` décidait "compte vide" (Bienvenue) vs contenu réel en se basant UNIQUEMENT sur `localStorage.sebaEntreprise` — un appareil sans ce cache (nouvel appareil, cache vidé) affichait "Bienvenue sur Seba" et invitait à recréer une entreprise déjà existante, même pour un vrai patron avec des données réelles côté serveur. Corrigé : vérifie désormais réellement le serveur via `SebaDB.pullFromServer()` (déjà existant, aucune nouvelle API) si le cache local est absent/invalide, avant de conclure qu'un compte est vide ; le chemin rapide (localStorage déjà valide) reste inchangé, aucune régression de vitesse pour le cas courant ; le cache est réchauffé après vérification. Testé en supprimant réellement le contournement localStorage utilisé jusqu'ici par les autres scripts QA de ce repo (connexion directe via `sebaAuth.signIn()`, navigation sans jamais écrire localStorage au préalable) — le contenu réel apparaît désormais correctement, jamais "Bienvenue". Aucune migration nécessaire (correctif frontend pur, réutilise une API déjà en production) | PR #105 (`fix(dashboard): verify server state before showing the empty-state gate`, mergée `be931255`), `scripts/qa-dashboard-empty-state-gate.js` (12/12, webkit+chromium), déployé et vérifié en production 2026-07-29, suites de régression (theme-layout, system-theme-empty-state, settings-brand-identity) re-passées sans régression |
| CED-001 | Customer Email Delivery — Phase 1 (backend) | commit `cba633b` |
| CED-002 | Customer Email Delivery — Phase 2 (interface) | commit `9e56065` |
| CED-003 | Activation réelle des comptes et accès (bootstrap `seba_state` + redirection par rôle sur `reset-password.html`) + `scripts/qa-account-activation.js` (20/20) — développé en local, **désormais sur `main` (AUTH-006) et validé sur le Supabase distant (AUTH-007)** | commit `67d9925` (dev local) → `cef4da0`/PR #95 (main) → validation distante ce jour |
| CED-DOC | Documentation des prérequis de déploiement (domaine/Resend) | commit `8b7d688`, `_architecture/DEPLOYMENT_CHECKLIST.md` |
| BILLING-001 | Devis/factures — modes simple/avancé + création flexible des documents commerciaux | commit `e8e7f53`, PR #93, `main` à `d2e4e77` |
| AUTH-002 | T2 — correction `create_profile_and_company` (idempotence secteur) | branche `fix/t2-onboarding-sector-idempotence`, fusionnée dans `main`, `migrations/2026-07-22-fix-t2-onboarding-sector-idempotence.sql` |
| AUTH-003 | T3 — fiabilité de la synchronisation (retry/recovery) | branche `fix/t3-sync-retry-recovery`, fusionnée dans `main` |
| AUTH-004 | Chevauchement `fix/invitation-delivery` vs `feature/customer-email-delivery` résolu SANS merge : diff isolé (`git diff 63fb0d9 fix/invitation-delivery`) montre que le socle (invitation, liaison compte, redirection par rôle) est déjà couvert et testé (67d9925, 20/20) ; le comportement réellement absent (statut de livraison réel + retry) dépend de `RESEND_API_KEY`, bloqué par la même cause que CED-004 — reporté en `AUTH-005`, branche non mergée conservée telle quelle, aucun code modifié | Décision documentée dans ce fichier (`AUTH-005` ci-dessus) ; commit `77fbb5f` |
| CED-009 | Draft PR ouverte pour `feature/customer-email-delivery` → `main`, description couvrant Phase 1/2 terminées, activation corrigée, blocage domaine, migrations/Edge Functions non déployées, automatisations reportées | PR #94 (`feat(email): add commercial document delivery`, **reste Draft, distincte de PR #95, toujours bloquée par CED-004** — ne contient plus le correctif d'activation, extrait dans AUTH-006) |
| CED-005 | Backend distant déployé sur le projet Supabase `ptmudezhxnhhyctowlqp` : migration `2026-07-28-commercial-email-delivery.sql` appliquée avec succès (confirmé fondateur), `send-commercial-document` déployée avec vérification JWT active (`verify_jwt:true`), `commercial-email-webhook` déployée avec `--no-verify-jwt` (protégée par sa propre vérification cryptographique de signature Resend/Svix, `verifySvixSignature` toujours présente dans le code) — aucun secret configuré, aucun envoi réel déclenché | `npx supabase functions list --project-ref ptmudezhxnhhyctowlqp` (les deux fonctions `ACTIVE`) |
| AUTH-006 | Correctif critique d'activation (bootstrap `seba_state` + redirection par rôle) extrait de `feature/customer-email-delivery` sur une branche/PR dédiée `fix/account-activation-bootstrap`, sans aucun code Customer Email Delivery (diff vérifié : 9 fichiers, `git diff --name-only main...HEAD`) ; migration corrigée pendant l'extraction pour ne pas régresser le durcissement T2 (SECURITY DEFINER/search_path/idempotence) ; `scripts/qa-account-activation.js` 20/20 réel (incluant un nouveau test explicite de retry idempotent) ; mergée dans `main` | PR #95 (`fix(auth): bootstrap new accounts and preserve role redirects`, Ready, mergée), commit `cef4da0` |
| AUTH-007 | Migration `2026-07-28-account-activation-bootstrap.sql` appliquée sur le projet Supabase distant `ptmudezhxnhhyctowlqp` (confirmé fondateur) et validée par un scénario réel de bout en bout contre ce projet (utilisateur QA préconfirmé, jamais d'email réel envoyé/lu) : profil+entreprise+`seba_state` créés en un seul appel, `seba_state` présent immédiatement (aucune dépendance à `sync-push`, qui n'est d'ailleurs pas déployée sur ce projet — hors périmètre de cette tâche), rejeu idempotent (même profil, aucun doublon), secteur différent explicitement refusé (code `23514`, durcissement T2 toujours actif), première écriture métier réelle persistée sans boucle 401, isolation cross-account vérifiée, toutes les données QA supprimées après coup (0 restante) | Script de validation exécuté contre `ptmudezhxnhhyctowlqp` ce jour, sortie `TOUT PASSE` (script ad hoc non commité, aucune donnée réelle touchée) |
| CORE-008 | `.github/workflows/static.yml` bloque désormais le déploiement Pages si `node tools/check-design-system.js` échoue — étape ajoutée après `Checkout`, avant `Setup Pages`/`Upload artifact`/`Deploy to GitHub Pages` ; aucun `actions/setup-node` ajouté (script Node pur, aucune dépendance) ; contrôle local réussi sur `main` avant modification ; run réel déclenché par le merge vérifié sur GitHub Actions — étape "Check design system" exécutée à la bonne position, déploiement terminé avec succès | PR #96 (`ci: block Pages deploy on design system violations`, Ready, mergée), commit `3677e8f`/merge `492dda3`, run GitHub Actions `30356076915` (succès) |
| QA360-001 | Campagne QA/UX 360° réelle sur `sebpromax.github.io/seba` (production, projet distant `ptmudezhxnhhyctowlqp`) — 5 agents bornés (patron, client+employé, cycle inter-rôles, chaos, novice isolé) + une passe de vérification indépendante par agent 11. **Aucun code produit corrigé** (hors périmètre assumé de cette mission) : constats classés, dédupliqués, preuves vérifiées. Constat le plus sévère, corroboré indépendamment par les 5 agents : `sync-push` n'est pas déployée en production, donc aucune écriture métier patron ne persiste réellement malgré un succès affiché côté UI — code déjà écrit (`supabase/functions/sync-push/index.ts`), jamais commité ni déployé ; extrait en `QA360-P0-A`. Autres constats retenus après vérification : invitation email 500 reproductible hors sandbox (`QA360-P0-B`), gate dashboard basé uniquement sur `localStorage` (`QA360-P1-A`), bannière 0€ côté client (`QA360-P2-A`), adresse manquante au détail mission employé (`QA360-P2-B`), texte "Compte de démonstration" figé (`QA360-P3-A`). Comptes/données QA (préfixe `QA360-A0X-`) entièrement nettoyés (0 résiduel vérifié sur les 4 comptes du projet distant), dossier `_tmp_qa/` supprimé | Vérification directe ce jour : `supabase functions list` (sync-push/ai-relay absentes), reproduction directe de l'échec d'invitation (500, aucun utilisateur orphelin), lecture directe de `docs/client-espace.html`/`migrations/2026-07-25-intervention-360.sql` confirmant les constats P2 ; requête admin API confirmant 0 compte `qa360-a0*` résiduel sur 4 comptes totaux |
| QA360-P0-A | Edge Function `sync-push` corrigée, déployée et validée en production réelle — plus aucune écriture patron ne se perd. **Historique** : trouvée non déployée par la campagne QA360-001 ; inspection du contrat a révélé que l'écriture (`entity_versions`) et la lecture (`seba_state.state`) n'avaient jamais été raccordées depuis le Palier 1 (2026-07-09) — déployer tel quel aurait donné un succès HTTP sans persistance réelle après reload. Corrigé : `apply_entity_patch()` réécrite pour écrire les deux atomiquement (verrouillage `FOR UPDATE` par compte). Revue pré-merge (PR #98) : allowlist de collections élargie (5 collections réellement utilisées manquaient depuis l'origine — contrats/custom_services/automationRules/automationRuns/automationAlerts), sémantique JSONB vérifiée exhaustivement (Seba envoie toujours des objets imbriqués complets, fusion superficielle suffisante), modèle de concurrence honnêtement documenté (sérialisation + dernier écrivain gagnant, pas un rejet de version), garde-fous ajoutés (id canonique obligatoire), sécurité vérifiée (RPC direct authenticated → 403). Revue post-merge (PR #99) : privilège EXECUTE de `service_role` rendu explicite (déjà présent en pratique via les défauts de plateforme, aucun appel n'avait jamais échoué — durcissement, pas correctif de panne). **Migrations appliquées sur `ptmudezhxnhhyctowlqp` par le fondateur** (2026-07-28, confirmé). **Déploiement + validation réelle sur `sebpromax.github.io/seba`** (2026-07-28) : vérifications distantes en lecture seule (signature à 5 paramètres seule présente, `service_role`=EXECUTE/`authenticated`+`anon`=aucun, 11 collections autorisées), `sync-push` déployée `ACTIVE` avec `verify_jwt=true`, aucune autre Edge Function redéployée (timestamps confirmés) ; cycle complet avec un patron QA réel depuis l'interface — client `QA-SYNC-PUSH-FINAL` créé, présent dans `seba_state.state` côté serveur, visible après reload complet ET après déconnexion/reconnexion (nouvelle session), modification d'un champ persistée après reload, aucun doublon, zéro erreur 401/403/500 sur tout le cycle, second patron QA confirmé sans accès au client du premier (isolation cross-account) ; toutes les données QA supprimées après coup (0 résiduel — comptes auth, `seba_state`, `entity_versions`) | PR #98 (`fix(sync): persist entity patches in account state`, mergée, commit `7f04278`) ; PR #99 (`fix(sync): grant patch execution to service role`, mergée, commit `e9d0381`) ; `scripts/local-db/test-sync-push-state-persistence.js` (20/20 local) ; validation production exécutée ce jour (scripts ad hoc non commités, données QA nettoyées) |
| DASH-EMPTY-001 | Un compte patron neuf n'affiche plus aucune donnée fictive. **Bug plus grave que prévu, trouvé en validant** : `SebaDB.ready()` (`docs/seba-data.js`) générait 6 faux clients + devis/factures/interventions/employés/journal dès la première visite et les **poussait réellement vers Supabase** (pas seulement un affichage) pour TOUT nouveau patron connecté — corrigé en gatant `seed()` sur `!hasSupabase` (jamais pour un compte réellement connecté). Complément : `DASHBOARD_DEMO_DATA` (fausses missions du jour) supprimé de `docs/app/dashboard.html`, remplacé par un bloc "Bienvenue dans Seba" + états vides réels déjà existants, interface complète conservée. **Validation PRODUCTION complète (2026-07-28, post-merge)** : déploiement Pages confirmé (fichiers réellement servis sur sebpromax.github.io contiennent le correctif — 0 `DASHBOARD_DEMO_DATA`, garde-fou `!hasSupabase` présent) ; patron QA neuf sur le projet distant réel — seba_state vide avant connexion, 0 donnée générée après attente+reload, bloc de bienvenue visible, 1 vrai client créé sans mélange ; compte QA avec vraies données — aucune régression (factures/CA réels affichés, aucun bloc de bienvenue, aucune donnée supprimée/remplacée) ; recherche de contamination historique (empreinte exacte des 6 noms positionnels de l'ancien seed()) sur les 3 comptes existants du projet distant — **AUCUNE CONTAMINATION DÉTECTÉE** (les 3 comptes ont 0 client, aucun ne correspond à l'empreinte) | PR #97 (`fix(dashboard): show real empty states for new accounts`, Ready, mergée), commit `0ae6906`/merge `606ed9c` ; validation production + scan de contamination exécutés ce jour (scripts ad hoc non commités, lecture seule pour le scan, données QA nettoyées pour les tests d'écriture) |

**Note de correction** : `_architecture/SEBA_OWNERS_AND_DEADLINES.md` (daté 2026-07-22) affiche encore les lignes 5 et 6 (T2/T3) comme "correction NON ASSIGNÉE" — vérifié obsolète : les deux branches sont fusionnées dans `main` (`git branch --merged main`). Ne pas se fier à ce document sans vérification Git — voir AUTH-002/AUTH-003 ci-dessus.
